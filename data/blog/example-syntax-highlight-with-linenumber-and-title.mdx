---
title: "Code Samples for the syntax highlighting"
date: "2023-05-07"
category: "Tech"
tags: ["unified", "sample"]
subtitle: "Some code blocks to check the syntax highlighting"
lastmod: "2023-05-07"
draft: false
metadescription: ""
image: ""
---

Please refer to the following page for instructions on how to display code titles and line numbers and highlight lines:
[timlrx/rehype-prism-plus: rehype plugin to highlight code blocks in HTML with Prism (via refractor) with line highlighting and line numbers](https://github.com/timlrx/rehype-prism-plus)
[rockchalkwushock/rehype-code-titles: Rehype plugin for parsing code blocks and adding titles to code blocks](https://github.com/rockchalkwushock/rehype-code-rockchalkwushock/rehype-code-titles)

---

## JavaScript with line number

```javascript showLineNumbers
function factorial(n) {
  if (n === 0) {
    return 1;
  } else {
    return n * factorial(n - 1);
  }
}

console.log(factorial(5)); // Output: 120
```

## CSS with code title & line number

```css:title showLineNumbers
/* Styles for a button */
.button {
  background-color: #3498db;
  border: none;
  color: white;
  padding: 12px 24px;
  text-align: center;
  text-decoration: none;
  display: inline-block;
  font-size: 16px;
  margin: 4px 2px;
  cursor: pointer;
}
```

## HTML with line highlighting

```html {1,3-4}
<!DOCTYPE html>
<html>
  <head>
    <title>My Website</title>
    <link rel="stylesheet" href="styles.css" />
  </head>
  <body>
    <h1>Welcome to my website!</h1>
    <p>Here is some text.</p>
    <script src="script.js"></script>
  </body>
</html>
```

## JSX with line highlighting & line number

```jsx {2,4-6} showLineNumbers
import React from "react";

function App() {
  return (
    <div>
      <h1>Hello, world!</h1>
      <p>This is a React component.</p>
    </div>
  );
}

export default App;
```

## Markup

```markup
<h1>Hello, world!</h1>
<p>This is some text.</p>
```

## Bash

```bash
#!/bin/bash

echo "Hello, world!"
```

## SCSS

```scss
$primary-color: #3498db;

.button {
  background-color: $primary-color;
  border: none;
  color: white;
  padding: 12px 24px;
  text-align: center;
  text-decoration: none;
  display: inline-block;
  font-size: 16px;
  margin: 4px 2px;
  cursor: pointer;
}
```

## TSX

```tsx
import React from "react";

interface Props {
  name: string;
}

function Hello({ name }: Props) {
  return <div>Hello, {name}!</div>;
}

export default Hello;
```

## TypeScript

```typescript
interface User {
  name: string;
  age: number;
}

function greet(user: User) {
  console.log(`Hello, ${user.name}! You are ${user.age} years old.`);
}

greet({ name: "Alice", age: 30 }); // Output: Hello, Alice! You are 30 years old.
```

## Go

```go
package main

import "fmt"

func main() {
    fmt.Println("Hello, world!")
}
```

## JSON

```json
{
  "name": "John",
  "age": 30,
  "city": "New York"
}
```

## Markdown

```markdown
This is some text.

# Heading 1

## Heading 2

### Heading 3

#### Heading 4

##### Heading 5

###### Heading 6

**Bold text**

_Italic text_

`Code`
```

## PHP

```php
<?php
  echo "Hello, world!";
?>
```

## Python

```python
def factorial(n):
    if n == 0:
        return 1
    else:
        return n * factorial(n-1)

print(factorial(5)) # Output: 120
```

## Regex

```regex
const re = /ab+c/;
const str = 'abc';
console.log(re.test(str)); // true

```

## Ruby

```ruby
def factorial(n)
  if n == 0
    1
  else
    n * factorial(n-1)
  end
end

puts factorial(5)
```

## SQL

```sql
SELECT id, name, email
FROM users
WHERE email LIKE '%example.com';
```

## YAML

```yaml
---
- name: Alice
  age: 24
  job: Engineer
- name: Bob
  age: 32
  job: Manager
- name: Carol
  age: 28
  job: Designer
```
